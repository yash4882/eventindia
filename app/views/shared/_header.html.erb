<%= stylesheet_link_tag "style"  %>

<nav class="navbar navbar-expand-lg shadow m-0" style="z-index: 99999;">
  <div class="container-fluid">
    <ul class="navbar-nav ">
      <li class="nav-item">
        <a class="navbar-brand " href=<%= root_path%>>eventINDIA</a>
      </li>
      <% if user_signed_in? %>
      <li class="nav-item">
        <div class="input-group rounded">
          <%#= search_form_for @q do |f| %>
          <%#= f.search_field :name_cont, type:"search", class:"form-control rounded", placeholder:"Search"%>
          <%# end %> 
        </div>
      </li>
      <%end%>
    </ul>

    <ul class="navbar-nav align-items-center ">
       <% if user_signed_in? %>
      <li class="nav-item create_event">
        <i class="bi bi-plus-lg"></i>
        <a href=<%= new_event_path %>>Create Events</a>
      </li>
      <% if current_user.has_role? "admin"%>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href=<%= root_path%>>Manage Users</a>
        </li>
      <% end %>
      <% end %>
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href=<%= root_path%>>Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link " href=<%=events_path%>>Events</a>
      </li>
      <li class="nav-item">
        <a class="nav-link " aria-current="page" href="/about">About</a>
      </li>
      <li class="nav-item">
        <a class="nav-link " aria-current="page" href="/contact">Contact</a>
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
          <div class="btn-group">
            <button type="button" class="btn border-0 dropdown-toggle" data-bs-toggle="dropdown">
              <%=current_user.email.capitalize%>
            </button>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href=<%=edit_user_registration_path%>>Profile</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "Sign-out", destroy_user_session_path,  data: {turbo_method: :delete,turbo_confirm: :"Are you sure?"},class:"dropdown-item"%></li>
            </ul>
          </div>
        <% else %>
        <%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary btn-sm mx-1" %> 
        <%= link_to "Sign-up",  new_user_registration_path, class: "btn btn-outline-primary btn-sm mx-1" %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
